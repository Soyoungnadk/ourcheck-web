# 매일 아침 6시(KST) 청약 공고·주변 시세를 다시 받아 페이지를 갱신한다.
#
# 브라우저는 공공데이터포털 API 를 CORS 로 직접 못 부른다.
# 그래서 여기서 대신 받아 index.html 에 구워 넣는다.
#
# 필요한 설정은 Secret 하나뿐이다:
#   Settings → Secrets and variables → Actions → New repository secret
#     이름: DATA_GO_KR_KEY
#     값  : 공공데이터포털 일반 인증키
#
# 지도 링크용 좌표를 붙이려면 KAKAO_REST_API_KEY 도 같은 방식으로 넣는다.
# 없어도 동작한다 — 좌표 대신 주소 검색 링크가 걸린다.
#
# 이 저장소 자신에게 push 하므로 GitHub 이 자동으로 주는 토큰으로 충분하다.
# (별도 PAT 을 만들 필요가 없다)

name: Daily Data

on:
  schedule:
    # Actions 는 UTC 기준. 21:00 UTC = 익일 06:00 KST
    - cron: '0 21 * * *'
  # 수집기가 바뀌면 데이터도 다시 구워야 한다. 새 항목을 넣어 놓고
  # 다음 날 아침까지 기다릴 이유가 없다.
  push:
    branches: [main]
    paths:
      - 'scripts/build_data.py'
      - '.github/workflows/daily-data.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-data
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 공고·시세 수집 후 페이지 갱신
        env:
          DATA_GO_KR_KEY: ${{ secrets.DATA_GO_KR_KEY }}
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
        run: python scripts/build_data.py --out index.html

      - name: 변경분 커밋
        run: |
          if git diff --quiet -- index.html; then
            echo "변경 없음 — 오늘은 새 공고가 없습니다."
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "chore: 공고·시세 자동 갱신 $(TZ=Asia/Seoul date +%Y-%m-%d)"
          git push
